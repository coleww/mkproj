var tap = {{#browserify}}require('tape'){{/browserify}}{{^browserify}}require('tap'){{/browserify}}

var {{camelName}} = require('./')

tap.test('does the thing', function (t) {
  t.plan(1)
  t.equal({{camelName}}('world'), 'hello world', 'does it')
})
