// get the directory argument

// make a directory by that name

// make index.js (nothing in it? JUST A LIL TOUCH)

// make README.md with dir name as header

// make index.html with boilerplate and dirname as title

// make main.css

// make .gitignore with /node_modules

// npm init (how to deal with opts?)

// install browserify watchify --save-dev

// add scripts to package.json

// git init, initial commit